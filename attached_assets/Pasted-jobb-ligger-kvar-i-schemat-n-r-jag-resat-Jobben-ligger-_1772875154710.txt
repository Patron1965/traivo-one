jobb ligger kvar i schemat när jag resat

Jobben ligger kvar visuellt efter att du rensat — det beror troligen på att frontend-cachen inte uppdateras korrekt. Låt mig kolla loggarna och koden:

Jag ser problemet i loggarna. Första rensningen tog bort 1056 jobb, men 1714 schemalagda jobb blev kvar. Det beror på att min säkerhetsfiltrering var för strikt — den rensade bara ordrar med status "draft", "scheduled" eller "pending", men de flesta ordrar i schemat har troligen andra statusar.

Låt mig kolla vilka statusar som faktiskt finns:

Nu ser jag! 28 172 schemalagda ordrar har status "completed" — det är dessa som ligger kvar. Min säkerhetsfilter exkluderade dem. Men "completed" ordrar med scheduledDate borde inte vara ett problem att avplanera om användaren vill rensa vyn.

Fixen: Byt strategi — istället för att lista vilka statusar som får rensas, exkludera bara fakturerade. Allt annat ska kunna rensas.

Inga ordrar har "invoiced" status som orderStatus. Det enklaste är att bara ta bort statusfiltret helt — rensning avplanerar alla schemalagda jobb i datumintervallet. De "completed" ordrarna ska kunna omplanerats om de har scheduledDate.

Nu behöver jag också hitta och fixa det som orsakar den långsamma 34336-items-requesten. Jag ser i loggarna att det fortfarande kommer en request som hämtar ALLA ordrar. Låt mig kolla vad som triggar den:

Problemet var att statusfiltret blockerade de flesta jobb (28172 hade status "completed"). Nu tar jag bort det filtret. Låt mig också se till att handleClearAllScheduled invaliderar cachen ordentligt:

Cache-invalidering ser bra ut — queryClient.invalidateQueries({ queryKey: ["/api/work-orders"] }) matchar alla work-order-queries. Problemet var helt i backend-filtret. Nu ska det fungera.

Låt mig också kolla om endDate-beräkningen är korrekt — veckan bör gå mån-fre:

Datumberäkningen ser rätt ut — fredag = måndag + 4 dagar. Och i storage.ts används lte(scheduledDate, endDate) vilket inkluderar fredagen. Men vänta — den server-sidan jämför med datumintervallet. Låt mig verifiera att lte med datum fungerar korrekt — om endDate är "2026-03-06" (fredag) bör alla jobb planerade på fredagen inkluderas:

Fixat! Statusfiltret som begränsade rensningen till bara "draft/scheduled/pending" är nu borttaget. Alla schemalagda jobb i veckointervallet kommer att avplaneras oavsett status.

Prova att rensa planeringen igen — nu bör alla jobb försvinna från schemat.


Scroll to latest

Make, test, iterate…
